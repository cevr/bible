/**
 * Study pane (§8).
 *
 * The verse-study bundle and the Strong's word-tap payload, as one portable
 * service over corpora the app already opens.
 */

export * from './model.js';
export * from './service.js';
