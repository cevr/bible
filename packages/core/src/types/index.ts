/**
 * Core Types
 *
 * Shared type definitions for the bible packages.
 */

export { CategoryId, categoryId, HymnId, hymnId, VerseId, verseId } from './ids.js';

export type {
  CategoryId as CategoryIdType,
  HymnId as HymnIdType,
  VerseId as VerseIdType,
} from './ids.js';
