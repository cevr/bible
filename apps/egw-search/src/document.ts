/**
 * The element the page mounts into. The server's document names it
 * (`../server/document.ts`) and the browser boot finds it (`./boot.tsx`),
 * both from here.
 */
export const rootId = 'root';
